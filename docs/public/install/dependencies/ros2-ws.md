# C-Extensions - Installation

```bash
cd Unitree-Go2-Control/c_extensions
pip install .
```

# ROS2 Workspace - Installation

## Install Build Dependencies

These are needed to run the `colcon` command in the CLI.

```bash
pip install colcon-common-extensions empy catkin_pkg lark-parser
```

